name: Test

on:
  push:
    paths:
      - '.github/workflows/test.yml'
      - 'resources/**'
      - 'sdk/**'
      - 'src/**'
      - 'stored_apps/**'
      - 'tests/**'
      - 'tools/**'
      - 'third_party/**'
      - 'waftools/**'
      - 'waf'
      - 'wscript'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/test.yml'
      - 'resources/**'
      - 'sdk/**'
      - 'src/**'
      - 'stored_apps/**'
      - 'tests/**'
      - 'tools/**'
      - 'third_party/**'
      - 'waftools/**'
      - 'waf'
      - 'wscript'
  workflow_dispatch:

jobs:
  test:
    if: |
      (github.event.repository.fork == true) ||
      (github.ref == 'refs/heads/main')
    strategy:
      fail-fast: false
      matrix:
        include:
          # snowy_bb2 - Pebble Time (baseline)
          - name: docker
            board: snowy_bb2
            runs-on: ubuntu-24.04
            container: ghcr.io/pebble-dev/pebbleos-docker:v1
            safe_directory: ${{ github.workspace }}
          - name: linux
            board: snowy_bb2
            runs-on: ubuntu-24.04
            safe_directory: ${{ github.workspace }}
          - name: macos
            board: snowy_bb2
            runs-on: macos-14
            safe_directory: /Users/runner/work/PebbleOS/PebbleOS
          # spalding_bb2 - Pebble Time Round (CHALK round form factor)
          - name: docker
            board: spalding_bb2
            runs-on: ubuntu-24.04
            container: ghcr.io/pebble-dev/pebbleos-docker:v1
            safe_directory: ${{ github.workspace }}
          - name: linux
            board: spalding_bb2
            runs-on: ubuntu-24.04
            safe_directory: ${{ github.workspace }}
          # silk_bb2 - Pebble 2 (DIORITE with HRM)
          - name: docker
            board: silk_bb2
            runs-on: ubuntu-24.04
            container: ghcr.io/pebble-dev/pebbleos-docker:v1
            safe_directory: ${{ github.workspace }}
          # asterix - Pebble 2 Duo (FLINT with nRF52840)
          - name: docker
            board: asterix
            runs-on: ubuntu-24.04
            container: ghcr.io/pebble-dev/pebbleos-docker:v1
            safe_directory: ${{ github.workspace }}

    name: test ${{ matrix.board }} ${{ matrix.name }}
    runs-on: ${{ matrix.runs-on }}
    container: ${{ matrix.container }}
    steps:
      - name: Mark Github workspace as safe
        run: |
          if [ "${{ matrix.name }}" = "linux" ]; then
            sudo git config --system --add safe.directory "${{ matrix.safe_directory }}"
          else
            git config --system --add safe.directory "${{ matrix.safe_directory }}"
          fi

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      # Native Linux: Install ARM toolchain and dependencies
      - name: Install dependencies (native Linux)
        if: matrix.name == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-none-eabi ninja-build gettext gcc-multilib g++-multilib libc6-dev-i386
          pip install -U pip
          pip install -r requirements.txt

      # macOS: Install ARM toolchain and dependencies
      - name: Install dependencies (macOS)
        if: matrix.name == 'macos'
        run: |
          brew install gcc-arm-embedded ninja
          python3 -m venv .venv
          source .venv/bin/activate
          pip install -U pip
          pip install -r requirements.txt

      # Docker: Already has dependencies, just update pip
      - name: Install Python dependencies (Docker)
        if: matrix.name == 'docker'
        run: |
          pip install -U pip
          pip install -r requirements.txt

      - name: Configure
        run: |
          if [ "${{ matrix.name }}" = "macos" ]; then
            source .venv/bin/activate
            ./waf configure --board ${{ matrix.board }} --js-engine=none --relax_toolchain_restrictions
          else
            ./waf configure --board ${{ matrix.board }} --js-engine=none
          fi

      - name: Run tests
        run: |
          if [ "${{ matrix.name }}" = "macos" ]; then
            source .venv/bin/activate
          fi
          ./waf test --continue_on_test_failure

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v5
        if: success() || failure()
        with:
          report_paths: ${{ github.workspace }}/build/test/junit.xml

      - name: Store failed test images
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: failed_diff_images-${{ matrix.name }}
          path: build/test/tests/failed/*-diff.png
